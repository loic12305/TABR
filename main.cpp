#include "TABR.cpp"

int main()
{


  generer_aleatoire(4,80);
  // TABR tabr;

       // initialiser(tabr);
       // parserFichier(tabr, "input.txt");
       // afficherT(tabr);
       // bool res = verifierTABR(tabr);
       // if(res)
       // 		cout << "Vrai" << endl;
       // 	else
       // 		cout << "Faux" << endl;

    return 0;
}
